# Ticket Booking System API

A Node.js + Express + Sequelize backend for an event ticket booking system.

## Tech Stack

- Node.js (ESM)
- Express
- Sequelize (MySQL)
- JWT-based authentication
- Role-based access control (ADMIN / USER)
- Temporary seat holds with expiry and booking confirmation

## Setup

1. Install dependencies:

```bash
npm install
